# Generated by IBM TransformationAdvisor
# Wed Apr 10 10:17:05 UTC 2019

apiVersion: v1
kind: Service
metadata:
  name: introwas
  labels:
    app: introwas
spec:
  ports:
    - port: 9080
  selector:
    app: introwas
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: introwas
  name: introwas
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: introwas
    spec:
      containers:
      - image: mycluster.icp:8500/default/introwas:latest
        name: introwas
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
